<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mis Reservaciones</title>
    <link rel="stylesheet" href="/css/reservationlist.css"> <!-- Enlazar el CSS -->
</head>
<body>
    <div class="card mx-auto" style="max-width: 800px;">
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h2>Mis Reservaciones</h2>
                <div>
                    <!-- Botón para hacer una nueva reservación -->
                    <a href="/reservations/new" class="btn btn-primary">Hacer Nueva Reservación</a>
                    <!-- Botón para cerrar sesión -->
                    <a href="/auth/logout" class="btn btn-secondary">Cerrar Sesión</a>
                </div>
            </div>

            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Nombre</th>
                        <th>Personas</th>
                        <th>Fecha</th>
                        <th>Hora</th>
                        <th>Correo</th>
                        <th>Acciones</th> <!-- Nueva columna para los botones de acción -->
                    </tr>
                </thead>
                <tbody>
                    {{#each reservations}}
                        <tr>
                            <td>{{this.name}}</td>
                            <td>{{this.people}}</td>
                            <td>{{this.date}}</td>
                            <td>{{this.time}}</td>
                            <td>{{this.email}}</td>
                            <td>
                                <!-- Botón para editar la reservación -->
                                <a href="/reservations/edit/{{this.id}}" class="btn btn-warning btn-sm">Editar</a>
                                <!-- Botón para eliminar la reservación -->
                                <form action="/reservations/delete/{{this.id}}" method="POST" style="display:inline;">
                                    <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                                </form>
                            </td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>
